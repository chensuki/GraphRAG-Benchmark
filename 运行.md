$env:LLM_API_KEY="sk-a81b8258c6014948a5867003537f8d9f"
$env:ZHIPUAI_API_KEY="7015f7527f6a4d14a57092f533c9f6cd.goT9SRRoyVfsvTLt"


# 完整运行（索引+查询）
python .\Examples\run_lightrag.py `
  --subset medical `
  --mode API `
  --model_name deepseek-chat `
  --llm_base_url https://api.deepseek.com/v1 `
  --embed_provider zhipu `
  --embed_model embedding-3 `
  --retrieve_topk 5 `
  --base_dir .\lightrag_workspace

# 仅运行查询（跳过索引，假设已索引完成）
python .\Examples\run_lightrag.py `
  --subset medical `
  --mode API `
  --model_name deepseek-chat `
  --llm_base_url https://api.deepseek.com/v1 `
  --embed_provider zhipu `
  --embed_model embedding-3 `
  --retrieve_topk 5 `
  --base_dir .\lightrag_workspace `
  --skip-build

# 仅运行查询（采样少量问题用于测试）
python .\Examples\run_lightrag.py `
  --subset medical `
  --mode API `
  --model_name deepseek-chat `
  --llm_base_url https://api.deepseek.com/v1 `
  --embed_provider zhipu `
  --embed_model embedding-3 `
  --retrieve_topk 5 `
  --base_dir .\lightrag_workspace `
  --skip-build `
  --sample 10

$env:PYTHONPATH="D:\Documents\AProject113\Context_engineering\KnowledgeGraph\GraphRAG-Benchmark\LightRAG"

  python .\Examples\run_lightrag.py --subset medical --mode API --model_name deepseek-chat --llm_base_url https://api.deepseek.com/v1 --embed_provider zhipu --embed_model embedding-3 --retrieve_topk 5 --base_dir .\lightrag_workspace

## ClearRAG 运行

# 方法1: 使用包装器脚本（推荐，自动使用 ClearRAG 虚拟环境）
.\Examples\run_clearrag_wrapper.ps1 --subset medical --config_path ClearRAG\config\config.yaml --sample 5

# 方法2: 直接使用 ClearRAG 虚拟环境中的 Python
ClearRAG\.venv\Scripts\python.exe Examples\run_clearrag.py --subset medical --config_path ClearRAG\config\config.yaml --sample 5

# 方法3: 使用项目根目录的虚拟环境（需要先安装依赖）
pip install -e ClearRAG/
python Examples\run_clearrag.py --subset medical --config_path ClearRAG\config\config.yaml --sample 5



  # 评估
  ## 检索评估
  python -m Evaluation.retrieval_eval `
  --mode API `
  --model deepseek-chat `
  --base_url https://api.deepseek.com/v1 `
  --embedding_model BAAI/bge-large-en-v1.5 `
  --data_file .\results\lightrag\Medical\predictions_Medical.json `
  --output_file .\results\retrieval_evaluation.json

  ## 生成评估

python -m Evaluation.generation_eval `
  --mode API `
  --model gpt-4o-mini `
  --base_url https://api.openai.com/v1 `
  --embedding_model BAAI/bge-large-en-v1.5 `
  --data_file .\results\lightrag\Medical\predictions_Medical.json `
  --output_file .\results\generation_evaluation.json

## 索引评估
python -m Evaluation.indexing_eval `
  --framework lightrag `
  --base_path .\lightrag_workspace `
  --folder_name Medical `
  --output .\results\indexing_metrics.txt


  python -m Evaluation.retrieval_eval `
  --mode API `
  --model deepseek-chat `
  --base_url https://api.deepseek.com/v1 `
  --embedding_model BAAI/bge-large-en-v1.5 `
  --data_file .\results\clearrag\predictions_Medical.json `
  --output_file .\results\retrieval_evaluation.json



## 生成回答
ClearRAG\.venv\Scripts\python.exe Examples\run_clearrag.py `
  --subset medical `
  --config_path ClearRAG\config\config.yaml `
  --skip-build `
  --top_k 5 `
  --sample 5